import { ErrorComponentWrapper } from "./ErrorComponent.style";

export const ErrorComponent = () => (
  <ErrorComponentWrapper>
    <p data-testid='error-component'>Oops! Looks like there's error!</p>
  </ErrorComponentWrapper>
)
